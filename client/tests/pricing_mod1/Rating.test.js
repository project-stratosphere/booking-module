import React from 'react';
import { shallow } from 'enzyme';
import Rating from '../../src/pricing_mod1/Rating.jsx';

test( 'Rating component should render as expected', () => {
  const component = shallow( <Rating /> );
} );
